class Mar():
	def __init__(self, side):
		self.order = 9
		self.value = 50
		self.name = "Mar9"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class Gen():
	def __init__(self, side):
		self.order = 8
		self.value = 40
		self.name = "Gen8"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class MGen():
	def __init__(self, side):
		self.order = 7
		self.value = 25
		self.name = "MGen7"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class BGen():
	def __init__(self, side):
		self.order = 6
		self.value = 15
		self.name = "BGen6"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class Col():
	def __init__(self, side):
		self.order = 5
		self.value = 8
		self.name = "Col5"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class Maj():
	def __init__(self, side):
		self.order = 4
		self.value = 5
		self.name = "Maj4"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class Capt():
	def __init__(self, side):
		self.order = 3
		self.value = 2
		self.name = "Capt3"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class Lt():
	def __init__(self, side):
		self.order = 2
		self.value = 1
		self.name = "Lt2"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class Spr():
	def __init__(self, side):
		self.order = 1
		self.value = 10
		self.name = "Spr1"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class Bomb():
	def __init__(self, side):
		self.order = None
		self.value = 18
		self.name = "Bomb"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class LMN():
	def __init__(self, side):
		self.order = 10
		self.value = 7
		self.name = "LMN"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"

class Flag():
	def __init__(self, side):
		self.order = 0
		self.value = 10000
		self.name = "Flag"
		self.side = side
		if self.side == "A":
			self.color = "dark orange"
		elif self.side == "B":
			self.color = "deep sky blue"








